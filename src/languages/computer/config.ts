import type { Exercise, LearningModule, WalkthroughStep } from '../../core/types'

type MatchAnswer = (source: string) => boolean
type LessonOptions = { walkthrough?: WalkthroughStep[]; checkpoints?: string[]; note?: string }
const normalise = (source: string) => source.trim().replace(/\s+/g, ' ')

const textExercise = (id: string, title: string, prompt: string, starter: string, solution: string, hints: string[], matches?: MatchAnswer): Exercise => ({
  id: `computer-${id}`,
  kind: title.includes('修复') ? 'repair' : title.includes('补全') ? 'complete' : 'create',
  title,
  prompt,
  starter,
  solution,
  hints,
  validate: (_value, source) => matches ? matches(source) : normalise(source) === normalise(solution),
})

const guides: Record<string, { steps: string; pitfalls: string; goals: string[] }> = {
  overview: { steps: '先确认报考科目、考试大纲和软件版本；再把公共基础与科目技能分开；每学完一块，用一个小任务验证；最后才用整套真题练速度。', pitfalls: '把不同科目的知识混成一门课；一开始就刷整套题；把“看过教程”误认为“能解释和操作”；为了完成进度条而机械做题。', goals: ['能说清自己报考科目的题型和软件环境', '能把学习目标写成可验收的动作', '能安排“理解—小练—复盘—限时”的学习闭环'] },
  basics: { steps: '先用位和字节解释数据大小；再画出 CPU、内存、外存之间的关系；最后用“谁管理资源、谁完成任务”给软件分类，并用一个命令或代码例子验证。', pitfalls: '混淆 bit 和 Byte；把 1000 与 1024 的换算场景混为一谈；把内存当成永久存储；把操作系统、编译器和普通应用软件混成一类。', goals: ['能完成二进制、字节和容量的基本换算', '能解释程序运行时 CPU、内存和外存各做什么', '能说明操作系统为什么不是普通应用软件'] },
  network: { steps: '先画“域名—DNS—IP—服务器”的请求路径；再区分 HTTP、HTTPS 和 DNS 各自负责什么；最后按机密性、完整性、可用性给风险配措施。', pitfalls: '把 DNS 当成加密服务；以为 HTTPS 能防所有诈骗；把备份当成防火墙；看到锁图标就认为网站内容一定可信。', goals: ['能按顺序解释一次网页访问的大致过程', '能判断 DNS、HTTP 和 HTTPS 的职责', '能针对账号、数据和服务风险选择合适措施'] },
  foundations: { steps: '先看元素之间的关系，再选数据结构；用小数组手算一次查找或排序；数循环执行次数，最后用增长趋势比较复杂度。', pitfalls: '看到“快”就直接选算法；把树的层级关系当成图；只背 O 记号不看输入规模；把平均情况、最坏情况和实际常数混为一谈。', goals: ['能从数据关系判断线性表、树或图', '能手算顺序查找和简单排序的一轮过程', '能比较 O(1)、O(log n)、O(n) 和 O(n²) 的增长趋势'] },
  word: { steps: '先建立标题、正文和列表的结构；再统一设置页面与样式；最后检查目录、分页、编号和文件格式。每一步都保留一次可回退的文件。', pitfalls: '用空格和回车“画”版式；只改选中的文字而不改样式；目录生成后不更新；最后才发现题目要求的是另一种文件格式。', goals: ['能用样式表达文档结构而不是手工堆格式', '能解释分页、段落和样式分别解决什么问题', '能按评分点完成并检查一份短文档'] },
  excel: { steps: '先看字段和数据范围；手算一行结果；写公式并确认引用；复制后抽查首行、末行和边界行；最后再排序、筛选和设置打印。', pitfalls: '只排序一列破坏记录对应关系；复制公式时固定条件单元格漂移；把数字存成文本；条件范围和求和范围没有对齐。', goals: ['能读懂相对、绝对和混合引用', '能写出常用统计公式并解释每个范围', '能在排序筛选前保护数据的行列关系'] },
  powerpoint: { steps: '先给每页写一句结论；再用版式组织标题、证据和行动建议；重复格式放入母版；最后在放映视图检查溢出、链接和动画。', pitfalls: '把讲稿整段塞进页面；逐页手动修改同一格式；用装饰代替论点；只看编辑视图不做一次完整放映。', goals: ['能把一段材料拆成有观点的页面结构', '能说明版式、主题和母版的区别', '能完成一次提交前的可读性和功能检查'] },
}

const lesson = (id: string, number: string, title: string, summary: string, sections: { heading: string; body: string }[], example: string, exercises: Exercise[], options: LessonOptions = {}): LearningModule['lessons'][number] => {
  const guide = guides[id]
  return { id, number, title, eyebrow: 'COMPUTER LEVEL 2', summary, sections: [...sections, { heading: '跟着做', body: guide.steps }, { heading: '常见误区', body: guide.pitfalls }], example, goals: guide.goals, exercises, ...options }
}

export const computerModule: LearningModule = {
  id: 'computer-level-2',
  name: '计算机二级入门',
  language: '计算机二级',
  editor: 'text',
  available: true,
  practiceRequired: false,
  description: '面向考生的公共基础、办公软件与程序设计入门。正文优先，练习只作为可选的理解验证。',
  lessons: [
    lesson('overview', '01', '考试地图：先确定你要学什么', '计算机二级不是一门固定内容的考试。先选主线，再决定哪些内容值得练。', [
      { heading: '学习目标', body: '学完这一课，你应该能回答：我报考的是哪一个科目？它的题型和软件环境是什么？哪些内容属于公共基础，哪些只属于 Word、Excel、PowerPoint、Python 或 C？如果这些问题还答不上来，就不适合立刻开始刷整套题。' },
      { heading: '把考试拆成三层', body: '第一层是公共基础：信息表示、硬件软件、网络安全、数据结构和算法。第二层是工具或语言：办公软件要求熟悉操作路径，程序设计要求理解语法和执行过程。第三层是考试策略：读题、保存、检查、计时。三层不能互相替代：会操作不等于懂原理，懂原理也不等于能在限定时间内完成。' },
      { heading: '为什么不建议一开始刷题', body: '刷题适合暴露问题，不适合代替建模。若还分不清“相对引用”和“绝对引用”，连续做十道 Excel 题往往只会记住十个答案。更有效的顺序是：先理解一个小概念，用一个小任务验证，再把它放进真题。' },
      { heading: '一份可执行的学习记录', body: '每次只记四件事：题目要求、我的做法、错在哪里、下次如何检查。例如“复制公式后税率变了；原因是 B1 没有锁定；下次复制前先判断哪些单元格代表固定规则”。这比记录“今天做了 30 题”更能指导下一次学习。' },
    ], '确认科目 → 建立模型 → 小任务验证 → 真题变式 → 限时复盘', [], { note: '本课没有硬性文字练习。请先在“无 AI 动手检查”里写出自己的报考主线，写不出来就先不要急着刷题。', checkpoints: ['不看资料，写出你的报考科目、软件版本和三类题型。', '把一个近期错题归入“概念 / 操作 / 粗心 / 时间”中的一类，并说明依据。', '把接下来一周安排成 3 次 30 分钟：理解、动手、复盘，而不是只安排刷题数量。'] }),
    lesson('basics', '02', '公共基础：计算机到底怎样表示和运行数据', '从 bit、字节到 CPU、内存和操作系统，建立“程序为什么能运行”的第一张图。', [
      { heading: '前置知识', body: '只需要小学到高中数学中的乘法、进位和单位换算，以及能看懂一条简单的命令。这里的“1 KB = 1024 Byte”是许多基础题采用的二进制换算；硬盘厂商和网络产品也可能使用十进制前缀，所以遇到真实产品要看标注。' },
      { heading: '从问题到设计', body: '计算机需要保存和处理各种信息，但电子电路最容易稳定地区分两种状态，于是用 0 和 1 表示位（bit）。单个位能表达的信息很少，通常把 8 个 bit 组织成 1 个字节（Byte），再用多个字节表示字符、整数、图片或程序。单位描述的是容量，不等于处理速度。' },
      { heading: '程序运行时谁在做什么', body: '外存保存文件，内存保存当前正在使用的数据和指令，CPU 从内存取指令并执行。内存断电后通常不保留普通工作数据，外存速度和用途不同。操作系统负责把这些资源管理起来，并向应用提供文件、进程、网络等接口；浏览器或表格软件则是在这个环境里完成具体任务。' },
      { heading: '严格说法与教学版说法', body: '教学版常说“CPU 负责计算，内存负责临时存储”，便于入门；严格一点说，CPU 还包含控制、寄存器和缓存等结构，内存也不是只有一种层级。二级入门阶段先掌握数据流和职责边界，再逐步补体系结构细节。' },
      { heading: 'C、Python 和 Linux 的联系', body: 'C 的 int、char 等类型直接对应一段确定大小的对象，但具体大小由实现决定；Python 的整数会自动扩展，许多表示细节被对象系统隐藏。Linux 中可以用 `free -h` 观察内存，用 `ls -lh` 观察文件大小；这两个命令的单位和含义不同，不能只看数字比较。' },
    ], '二进制：1011₂ = 1×2³ + 0×2² + 1×2¹ + 1×2⁰ = 11₁₀\n8 bit = 1 Byte\n程序：外存文件 → 装入内存 → CPU 取指执行', [
      textExercise('basics-1', '手算容量', '不看答案，写出 3 Byte 等于多少 bit。', '', '3 Byte = 24 bit', ['1 Byte 由 8 bit 组成。', '先算 3 × 8。', '答案要带单位。'], (source) => /3\s*byte\s*=\s*24\s*bit/i.test(source.replace(/，/g, ''))),
      textExercise('basics-2', '解释职责', '用一句话说明“操作系统”和“应用软件”的区别。可以使用自己的话。', '', '操作系统管理资源并提供运行环境；应用软件完成具体任务', ['先写操作系统负责什么。', '再写应用软件负责什么。', '不要求固定顺序，但要同时提到资源/运行环境和具体任务。'], (source) => /操作系统/.test(source) && /(资源|运行环境)/.test(source) && /应用软件/.test(source) && /具体任务/.test(source)),
    ], { walkthrough: [{ code: '1011₂', explanation: '从右到左的权值是 2⁰、2¹、2²、2³。每一位只在对应数字为 1 时贡献权值。' }, { code: '1×8 Byte', explanation: '8 bit 才组成 1 Byte，所以 3 Byte 先换成 3×8=24 bit。不要把 bit 的小写 b 和 Byte 的大写 B 混写。' }, { code: '程序启动', explanation: '文件先位于外存；启动后相关代码和数据进入内存，CPU 才能按指令读取并执行。' }], checkpoints: ['手算 1101₂ 转十进制，并写出每一位的权值。', '打开终端运行 `free -h` 和 `ls -lh`，说出它们分别描述什么。', '解释为什么关闭一个程序后，相关内存可以被系统重新分配。'], note: '不要把“容量更大”直接理解成“速度更快”。基础题最常考的是单位、职责和数据流。' }),
    lesson('network', '03', '公共基础：网络与安全不是背缩写', '把一次网页访问拆成可追踪的步骤，再把风险和保护措施一一对应。', [
      { heading: '先画出请求路径', body: '人在地址栏输入域名，但网络设备需要 IP 地址。DNS（域名系统）负责把域名解析为 IP；解析完成后，客户端才会向目标服务器发起连接和请求。DNS 解决的是“找到谁”，不是“把内容加密”。' },
      { heading: 'HTTP、HTTPS 和 DNS 的分工', body: 'HTTP 描述浏览器和服务器如何请求、响应 Web 资源；HTTPS 是在 HTTP 之上增加 TLS 保护的使用方式，重点是传输机密性、完整性和服务器身份校验。它不能保证网页运营者说的每句话都是真的，也不能代替用户识别钓鱼网站。' },
      { heading: '安全三要素', body: '机密性：不该看到的人看不到，例如传输加密；完整性：数据没有被悄悄改掉，例如校验和、签名；可用性：需要时服务能工作，例如备份、冗余和限流。遇到安全题，先问“要保护什么、面对什么风险”，再选措施。' },
      { heading: '现实中的判断', body: '复杂密码和多因素认证主要降低账号被猜中或盗用的风险；备份主要应对误删、损坏和勒索后的恢复；更新系统减少已知漏洞被利用；不打开可疑附件减少钓鱼和恶意代码入口。一个措施通常只解决一部分问题。' },
    ], '输入域名 → DNS 得到 IP → 建立连接 → HTTPS 请求 → 服务器响应 → 浏览器渲染', [
      textExercise('network-1', '匹配风险', '为“误删重要文件”写出最直接的保护措施。', '', '定期备份数据', ['密码主要保护账号。', '误删后需要恢复副本。', '答案包含“备份”。'], (source) => /(备份|副本|恢复)/.test(source)),
      textExercise('network-2', '说清分工', '用一句话区分 DNS 与 HTTPS。', '', 'DNS 把域名解析为 IP；HTTPS 保护 Web 通信', ['DNS 负责找到地址。', 'HTTPS 负责保护通信。', '顺序不限，但两部分都要出现。'], (source) => /DNS/i.test(source) && /(域名|名字)/.test(source) && /(IP|地址)/i.test(source) && /HTTPS/i.test(source) && /(加密|通信|保护)/.test(source)),
    ], { walkthrough: [{ code: 'example.com', explanation: '这是人容易记的域名，不是服务器本身的硬件地址。' }, { code: 'DNS → 93.184.216.34', explanation: 'DNS 返回解析结果；实际环境可能有缓存、多个地址和 CDN，基础模型先抓住“名字到地址”。' }, { code: 'HTTPS request', explanation: '连接建立后，浏览器通过 HTTPS 保护请求和响应的传输过程。' }], checkpoints: ['不看资料，画出一次网页访问的 5 个步骤。', '分别给“撞库、误删、网页被篡改、服务器宕机”配一个主要措施。', '在 Linux 中运行 `ping` 或 `nslookup`（若可用），观察它们更接近路径中的哪一步。'], note: '看到“安全”不要直接选“加密”。先判断题目保护的是账号、内容、真实性还是可用性。' }),
    lesson('foundations', '04', '公共基础：数据结构与算法的思考方式', '不靠“哪个更高级”的印象选答案，而是从数据关系和执行次数推导。', [
      { heading: '先问数据之间是什么关系', body: '线性表中的元素有前后顺序；树用父子关系表示层级，例如文件目录；图用节点和边表示任意连接，例如城市道路或社交关系。一个结构不因名字听起来复杂就更合适，关键是它是否表达了题目中的关系。' },
      { heading: '从顺序查找推导复杂度', body: '顺序查找从第一个元素开始比较。最好一次就找到，最坏要比较 n 次，平均比较次数与 n 同阶，因此常写 O(n)。如果每次都把待查范围缩小一半，比较次数大约是 log₂n，才会出现 O(log n) 的增长。' },
      { heading: '双重循环为什么常见 O(n²)', body: '若外层循环执行 n 次，内层每次也执行 n 次，总次数约为 n×n。常数、低阶项和具体硬件会影响真实耗时，但输入规模变大时，平方级增长通常比线性级更快变慢。复杂度是增长趋势，不是精确秒数。' },
      { heading: 'C、Python 与实际选择', body: 'C 数组让连续内存和下标访问更直观；Python 列表提供了更高层的容器接口，但底层仍需要保存元素和管理内存。写程序时先说清操作：是否频繁按下标访问、是否需要保持顺序、是否需要按关系遍历，再选结构。' },
    ], '顺序查找 [7, 2, 9, 4]\n查找 9：比较 7 → 2 → 9，共 3 次\n最坏情况：O(n)', [
      textExercise('foundations-1', '判断增长', '按“增长慢到快”的顺序写出 O(1)、O(log n)、O(n)、O(n²)。', 'O(1) < ', 'O(1) < O(log n) < O(n) < O(n²)', ['常数级不随 n 增长。', '对数级通常慢于常数、快于线性。', '平方级放在最后。'], (source) => ['O(1)', 'O(log n)', 'O(n)', 'O(n²)'].every((token, index, tokens) => source.indexOf(token) >= 0 && (index === 0 || source.indexOf(token) > source.indexOf(tokens[index - 1])))),
      textExercise('foundations-2', '解释结构', '说明为什么文件目录通常适合用树表示。', '', '文件目录有父子层级关系，树能表达这种层级', ['关键词是父子或层级。', '不要只写“树很快”。', '说清“能表达”。'], (source) => /(目录|文件)/.test(source) && /(父子|层级)/.test(source) && /(树|表示|表达)/.test(source)),
    ], { walkthrough: [{ code: 'i = 0', explanation: '从第一个元素开始，i 每次向后移动一个位置。' }, { code: 'a[i] == target?', explanation: '每次比较可能成功，也可能继续；最坏要走完整个数组。' }, { code: 'n 次 → O(n)', explanation: '忽略常数和低阶项后，用 O(n) 表示输入规模增长时的主要趋势。' }], checkpoints: ['手画一个 4 层文件目录树，并说出它为什么不是线性表。', '数出两个嵌套循环各执行 5 次时的总次数。', '解释为什么“同为 O(n)”不代表两个程序在真实机器上一样快。'], note: '复杂度是帮助你比较增长趋势的语言，不是给每个程序贴上的永久速度标签。' }),
    lesson('word', '05', 'Word：先建立结构，再调整外观', '把 Word 操作题理解成“结构化文档的构建”，而不是逐字摆放。', [
      { heading: '为什么要用样式', body: '标题 1、标题 2、正文等样式不仅改变字号和颜色，还携带了文档结构。目录、导航窗格、批量修改和辅助技术都依赖这些结构信息。连续空格和回车只能改变当前版面，不能告诉 Word“这是一个标题”。' },
      { heading: '一份文档的操作顺序', body: '先另存为题目要求的文件名；输入或检查文本；按层级应用标题样式；设置页面、页眉页脚、段落和列表；插入表格或图片；更新目录；最后从头到尾检查分页、编号、溢出和保存位置。顺序的核心是先让文档“有意义”，再让它“好看”。' },
      { heading: '容易混淆的三个层次', body: '字符格式作用于选中的文字，段落格式作用于段落，样式是可复用的格式集合。调整一个标题的颜色，不等于修改了所有同级标题；修改标题样式，才是在修改规则。' },
      { heading: '无 AI 验收', body: '不看步骤，拿一段三层标题的文字，能在 10 分钟内建立可更新目录，并能说明自己没有使用空格和回车模拟层级，就达到这一课的基础目标。' },
    ], '标题 1：计算机二级入门\n  标题 2：公共基础\n    正文：先建立结构，再统一调整外观。\n目录：由标题层级自动生成', [
      textExercise('word-1', '说明原则', '写出为什么标题应使用标题样式，而不是连续空格。', '', '标题样式保存层级结构，可用于目录和统一修改', ['要出现“层级/结构”。', '再说目录或统一修改中的至少一项。', '用自己的话即可。'], (source) => /样式/.test(source) && /(层级|结构)/.test(source) && /(目录|统一|修改)/.test(source)),
    ], { checkpoints: ['创建一个含标题 1、标题 2 和正文的短文档。', '生成目录后修改一个标题，更新目录，观察变化。', '故意用空格模拟标题，对比导航窗格能否识别它。'], note: 'Word 练习的重点不是记住某个按钮在哪，而是知道每个按钮在修改哪一层结构。' }),
    lesson('excel', '06', 'Excel：公式、引用与数据关系', '先理解单元格引用，再谈函数、填充、排序和筛选。', [
      { heading: '公式不是答案，而是规则', body: '在 C2 写 =A2*B2，表达的是“本行数量乘以本行单价”。向下填充时，A2 和 B2 会相对移动到 A3、B3。公式的价值在于保存计算规则，数据变化后可以重新计算，而不是只存一串结果。' },
      { heading: '相对、绝对和混合引用', body: '相对引用 A1 会随复制移动；绝对引用 $A$1 的行列都固定；混合引用 $A1 或 A$1 只固定一部分。判断是否要加 $，先问这个单元格代表“当前记录”还是“所有记录共用的规则”，例如税率、汇率和阈值通常要锁定。' },
      { heading: '函数和范围要一起读', body: 'SUM(B2:B6) 把 B2 到 B6 的值相加；AVERAGE 求平均；COUNTIF 根据条件计数。读公式时不要只背函数名，要检查起止范围、条件文本和数据类型。排序与筛选前选中完整数据区域，否则姓名、日期和金额可能错位。' },
      { heading: '无 AI 验收', body: '给自己建一张 5 行的小表：数量、单价、税率、含税价。能写出含税价公式，向下填充后税率不漂移，并能解释每个 $ 的作用，就说明引用这一节真正懂了。' },
    ], '=B2*C2*(1+$F$1)\nB2、C2：当前行数据\n$F$1：所有行共用的税率', [
      textExercise('excel-1', '锁定规则', '把 =B2*C2*(1+F1) 改成向下填充时税率不变的公式。', '=B2*C2*(1+F1)', '=B2*C2*(1+$F$1)', ['B2 和 C2 要随行变化。', 'F1 是固定规则。', '只锁定 F1 的列和行。']),
      textExercise('excel-2', '检查范围', '写出统计 D2 到 D10 平均值的公式。', '', '=AVERAGE(D2:D10)', ['平均值函数是 AVERAGE。', '范围从 D2 到 D10。', '公式以等号开始。']),
    ], { walkthrough: [{ code: '=B2*C2', explanation: 'B2、C2 没有 $，复制到下一行会变成 B3、C3，因为它们代表当前记录。' }, { code: '*(1+$F$1)', explanation: 'F1 是全表共用的税率，所以行列都锁定；复制后仍指向同一个规则。' }, { code: '排序前选整表', explanation: '一条记录的字段必须一起移动。只选金额列排序，会让姓名和金额失去对应关系。' }], checkpoints: ['复制一条含相对引用和绝对引用的公式，逐格写出变化结果。', '把一个数字改成文本，观察 SUM 或 AVERAGE 是否仍按预期工作。', '先只选一列排序，再撤销；说明为什么这是危险操作。'], note: 'Excel 最值得理解的不是函数数量，而是“规则如何引用数据、复制后是否仍指向正确对象”。' }),
    lesson('powerpoint', '07', 'PowerPoint：把信息组织成观点', '幻灯片不是 Word 的缩小版，也不是讲稿的截图。', [
      { heading: '一页只承担一个重点', body: '标题先给结论，正文或图表提供证据，最后必要时给出建议。若一页同时讲现象、原因、历史和方案，观众很难判断重点。先写一句“看完这一页要记住什么”，再决定放哪些内容。' },
      { heading: '版式、主题和母版', body: '版式决定这一页的占位结构；主题统一字体、颜色和效果；母版控制多页重复出现的元素，如页脚、标志和标题位置。需要全局一致的内容放在母版，需要单页例外的内容留在页面上。' },
      { heading: '从材料到页面的推导', body: '一段材料通常包含事实、解释和行动。可以拆成“发生了什么—为什么—接下来做什么”，但这不是死规则；如果听众只需要比较两个方案，就应使用对比表而不是强行套三段式。结构服务于沟通目的。' },
      { heading: '无 AI 验收', body: '拿一段 150 字材料，独立做 3 页：结论页、证据页、建议页。放映时能在 5 秒内说出每页重点，且没有文字溢出、图片变形和失效链接，就达到基础目标。' },
    ], '标题：本月故障率下降\n证据：按版本的故障率对比图\n建议：优先修复旧版本的两个高频问题', [
      textExercise('powerpoint-1', '写出页面结构', '用“现象 / 原因 / 建议”写出一页汇报的三段结构。', '', '现象 / 原因 / 建议', ['第二项解释为什么。', '第三项应该能指导行动。', '使用斜杠分隔即可。']),
    ], { checkpoints: ['把一页塞满的讲稿删到 3 个要点，每个要点不超过一行。', '用母版统一两页的页脚，再只修改一页的局部内容。', '完整放映一次，记录自己在哪一页需要停下来找重点。'], note: '“少”不是审美要求，而是为了让听众看出信息层次。讲稿可以很长，页面不必替你说完所有话。' }),
  ],
}
