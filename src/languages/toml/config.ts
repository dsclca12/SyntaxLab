import type { LearningModule } from '../../core/types'
import { matchesObject } from '../../core/validator'

import type { ExerciseKind } from '../../core/types'
const ex = (id: string, title: string, prompt: string, starter: string, solution: string, expected: Record<string, unknown>, hints: string[], kind: ExerciseKind = title.toLowerCase().includes('repair') ? 'repair' : 'create') => ({ id, kind, title, prompt, starter, solution, hints, validate: (value: unknown) => matchesObject(value, expected) })
const basic = (id: string, title: string, prompt: string, starter: string, solution: string, expected: Record<string, unknown>) => ex(id, title, prompt, starter, solution, expected, ['Look at the key name first.', 'A string needs quotes; numbers and booleans do not.', 'Try the example, then adapt it to the prompt.'], 'complete')

export const tomlModule: LearningModule = {
  id: 'toml', name: 'TOML', language: 'TOML', available: true, description: 'Configuration you can actually understand.', lessons: [
    { id: 'intro', number: '01', title: 'What is TOML?', eyebrow: 'START HERE', summary: 'A calm, readable format for configuration.', sections: [{ heading: 'Why TOML?', body: 'TOML stands for Tom\'s Obvious, Minimal Language. It is designed for humans to read and write, while remaining precise for programs.' }, { heading: 'Where you will see it', body: 'Project metadata, build tools and application settings often use TOML. Python projects use pyproject.toml; Rust projects use Cargo.toml.' }], example: 'name = "demo"\nversion = "1.0.0"', note: 'TOML is configuration, not a general-purpose programming language.', exercises: [basic('intro-1', 'A first config', 'Create a project named demo.', 'name = ', 'name = "demo"', { name: 'demo' }), ex('intro-2', 'Repair the config', 'Fix the missing quotes.', 'name = demo', 'name = "demo"', { name: 'demo' }, ['Text values need quotes.', 'The parser treats bare words as invalid values.', 'Use basic string syntax: "text".']), basic('intro-3', 'Add a version', 'Create a project named app with version 1.0.0.', '', 'name = "app"\nversion = "1.0.0"', { name: 'app', version: '1.0.0' })] },
    { id: 'key-value', number: '02', title: 'Key / Value', eyebrow: 'FOUNDATIONS', summary: 'Every setting starts with a name and a value.', sections: [{ heading: 'The assignment shape', body: 'A key identifies a setting. The equals sign separates it from its value. Whitespace around = is optional, but spaces improve scanning.' }, { heading: 'Comments', body: 'A # starts a comment that runs to the end of the line. Comments explain intent without changing the parsed data.' }], example: '# service settings\nenabled = true\nname = "api"', exercises: [basic('kv-1', 'Enable a service', 'Set enabled to true.', 'enabled = ', 'enabled = true', { enabled: true }), ex('kv-2', 'Repair a key/value', 'Fix the separator and value.', 'name: api', 'name = "api"', { name: 'api' }, ['TOML uses =, not :.','The value is a string.','Parse it again after editing.']), basic('kv-3', 'Write two settings', 'Create debug = false and retries = 3.', '', 'debug = false\nretries = 3', { debug: false, retries: 3 })] },
    { id: 'types', number: '03', title: 'Data types', eyebrow: 'FOUNDATIONS', summary: 'TOML has explicit, useful primitive types.', sections: [{ heading: 'A small set of clear types', body: 'Strings, integers, floats, booleans, dates and times are distinct values. This makes configuration predictable for the program reading it.' }, { heading: 'Read the value', body: 'Quotes signal text. A decimal point signals a float. true and false are booleans, not strings.' }], example: 'title = "SyntaxLab"\ncount = 12\nratio = 0.75\nactive = true', exercises: [basic('types-1', 'Mix types', 'Create count = 3 and ratio = 1.5.', '', 'count = 3\nratio = 1.5', { count: 3, ratio: 1.5 }), ex('types-2', 'Repair the boolean', 'Make active a boolean, not text.', 'active = "true"', 'active = true', { active: true }, ['Remove the quotes.', 'Boolean literals are lowercase.', 'The parsed value should be true, not the string "true".']), basic('types-3', 'Describe a build', 'Create name = "release" and ready = false.', '', 'name = "release"\nready = false', { name: 'release', ready: false })] },
    { id: 'string', number: '04', title: 'String', eyebrow: 'VALUES', summary: 'Represent text safely, including quotes and line breaks.', sections: [{ heading: 'Basic and literal strings', body: 'Basic strings use double quotes and support escapes such as \\n. Literal strings use single quotes when you want backslashes to stay literal.' }, { heading: 'Multiline strings', body: 'Triple quotes hold longer text across lines. Use them for descriptions or embedded snippets.' }], example: 'name = "hello"\npath = \'C:\\\\temp\'\ndescription = """A\nshort note."""', exercises: [basic('str-1', 'Write a greeting', 'Create greeting = "hello".', '', 'greeting = "hello"', { greeting: 'hello' }), ex('str-2', 'Repair the string', 'Fix the unterminated string.', 'message = "hello', 'message = "hello"', { message: 'hello' }, ['A basic string needs an opening and closing double quote.', 'Keep hello inside the quotes.', 'Then run Check answer.']), basic('str-3', 'Store a path', 'Create path = "./data".', '', 'path = "./data"', { path: './data' })] },
    { id: 'number', number: '05', title: 'Number', eyebrow: 'VALUES', summary: 'Integers, floats, signs and readable separators.', sections: [{ heading: 'Numbers are values', body: 'Integers have no decimal point; floats do. You can use + or - signs and underscores as visual separators.' }, { heading: 'Keep the meaning', body: '1_000 is the same number as 1000. Quotes would turn a number into text, which changes how tools can use it.' }], example: 'workers = 4\ntimeout = 2.5\nbudget = 1_000', exercises: [basic('num-1', 'Set a port', 'Create port = 8080.', '', 'port = 8080', { port: 8080 }), ex('num-2', 'Repair the number', 'Make timeout a number.', 'timeout = "2.5"', 'timeout = 2.5', { timeout: 2.5 }, ['Remove quotes from numeric values.', 'A decimal is a float.', 'The expected value is 2.5.']), basic('num-3', 'Set a limit', 'Create limit = -10.', '', 'limit = -10', { limit: -10 })] },
    { id: 'boolean', number: '06', title: 'Boolean', eyebrow: 'VALUES', summary: 'Two words for on/off decisions.', sections: [{ heading: 'Only two values', body: 'TOML booleans are true and false. They are lowercase and never wrapped in quotes.' }], example: 'enabled = true\ndebug = false', exercises: [basic('bool-1', 'Turn it on', 'Set enabled to true.', '', 'enabled = true', { enabled: true }), ex('bool-2', 'Repair debug', 'Make debug a boolean false.', 'debug = "false"', 'debug = false', { debug: false }, ['This is a boolean exercise.', 'Remove the quotes.', 'Use lowercase false.']), basic('bool-3', 'Write feature flags', 'Create beta = true and logs = false.', '', 'beta = true\nlogs = false', { beta: true, logs: false })] },
    { id: 'array', number: '07', title: 'Array', eyebrow: 'STRUCTURE', summary: 'Keep related values together in a predictable order.', sections: [{ heading: 'An ordered list', body: 'Arrays use square brackets and commas. Values can span lines, and arrays may contain strings, numbers or nested arrays.' }, { heading: 'Type discipline', body: 'Keep array items compatible. A list of ports should contain numbers; a list of hosts should contain strings.' }], example: 'ports = [80, 443, 8080]\nhosts = [\n  "api.example.com",\n  "cdn.example.com"\n]', exercises: [basic('arr-1', 'List ports', 'Create ports with 80 and 443.', '', 'ports = [80, 443]', { ports: [80, 443] }), ex('arr-2', 'Repair the array', 'Fix the missing closing bracket.', 'ports = [80, 443', 'ports = [80, 443]', { ports: [80, 443] }, ['Arrays open with [ and close with ].', 'Separate items with commas.', 'Check that the parsed value is a list.']), basic('arr-3', 'Write supported formats', 'Create formats = ["json", "toml"].', '', 'formats = ["json", "toml"]', { formats: ['json', 'toml'] })] },
    { id: 'table', number: '08', title: 'Table', eyebrow: 'STRUCTURE', summary: 'Group related keys under a named table.', sections: [{ heading: 'The table is a namespace', body: 'A table header such as [server] opens a namespace. Keys that follow belong to the server object until another table is declared.' }, { heading: 'Why it matters', body: 'Tables turn flat settings into a meaningful tree. The parser returns { server: { ... } }, which mirrors how your application thinks about the config.' }], example: '[server]\nhost = "127.0.0.1"\nport = 8080', exercises: [ex('table-1', 'Create a server', 'Create a [server] table with port 8080.', '', '[server]\nport = 8080', { server: { port: 8080 } }, ['A table header goes in square brackets.', 'Put port after the header.', 'port is an integer.']), ex('table-2', 'Repair the table', 'Fix the table header.', '[server\nhost = "localhost"', '[server]\nhost = "localhost"', { server: { host: 'localhost' } }, ['The header needs both brackets.', 'The key follows the header.', 'Keep localhost as a string.']), basic('table-3', 'Describe an app', 'Create [app] with name = "web".', '', '[app]\nname = "web"', { app: { name: 'web' } })] },
    { id: 'nested', number: '09', title: 'Nested Table', eyebrow: 'STRUCTURE', summary: 'Use dotted headers to express deeper configuration.', sections: [{ heading: 'Dotted paths', body: 'A header such as [database.primary] creates database, then primary inside it. This is a compact way to express nested objects.' }], example: '[database.primary]\nhost = "localhost"\nport = 5432', exercises: [basic('nested-1', 'Connect a database', 'Create [database.primary] with port 5432.', '', '[database.primary]\nport = 5432', { database: { primary: { port: 5432 } } }), ex('nested-2', 'Repair a path', 'Fix the nested header.', '[database,primary]\nhost = "db"', '[database.primary]\nhost = "db"', { database: { primary: { host: 'db' } } }, ['Nested table names use dots.', 'Do not use a comma in the header.', 'The result should have two object levels.']), basic('nested-3', 'Add a replica', 'Create [database.replica] with host = "replica".', '', '[database.replica]\nhost = "replica"', { database: { replica: { host: 'replica' } } })] },
    { id: 'array-table', number: '10', title: 'Array of Tables', eyebrow: 'STRUCTURE', summary: 'Represent repeated records with double brackets.', sections: [{ heading: 'A list of objects', body: '[[servers]] starts one table in an array. Repeating the header adds another object to the same list.' }, { heading: 'Think in records', body: 'This is useful for servers, users or plugins: each repeated block describes one item with the same shape.' }], example: '[[servers]]\nname = "server-a"\nip = "10.0.0.1"\n\n[[servers]]\nname = "server-b"\nip = "10.0.0.2"', exercises: [ex('aot-1', 'Add two servers', 'Create two servers named a and b.', '', '[[servers]]\nname = "a"\n\n[[servers]]\nname = "b"', { servers: [{ name: 'a' }, { name: 'b' }] }, ['Use double square brackets.', 'Repeat the same header for the second record.', 'The parsed value is an array.']), ex('aot-2', 'Repair the records', 'Turn the second table into another servers item.', '[[servers]]\nname = "a"\n\n[servers]\nname = "b"', '[[servers]]\nname = "a"\n\n[[servers]]\nname = "b"', { servers: [{ name: 'a' }, { name: 'b' }] }, ['Both blocks describe items in the same array.', 'Use [[servers]] twice.', 'A single bracket creates an object, not a list.']), basic('aot-3', 'Write a service list', 'Create two services named api and web.', '', '[[services]]\nname = "api"\n\n[[services]]\nname = "web"', { services: [{ name: 'api' }, { name: 'web' }] })] },
    { id: 'inline', number: '11', title: 'Inline Table', eyebrow: 'STRUCTURE', summary: 'Keep a tiny object close to the key that uses it.', sections: [{ heading: 'Compact objects', body: 'Inline tables use braces and comma-separated key/value pairs. They are ideal for small, self-contained structures.' }], example: 'point = { x = 1, y = 2 }', exercises: [basic('inline-1', 'Create a point', 'Create point with x = 1 and y = 2.', '', 'point = { x = 1, y = 2 }', { point: { x: 1, y: 2 } }), ex('inline-2', 'Repair the inline table', 'Add the missing closing brace.', 'point = { x = 1, y = 2', 'point = { x = 1, y = 2 }', { point: { x: 1, y: 2 } }, ['Inline tables open with { and close with }.', 'Separate fields with commas.', 'Keep x and y as numbers.']), basic('inline-3', 'Write coordinates', 'Create size with width = 10 and height = 20.', '', 'size = { width = 10, height = 20 }', { size: { width: 10, height: 20 } })] },
    { id: 'datetime', number: '12', title: 'Date / Time', eyebrow: 'VALUES', summary: 'Store moments and calendar dates in a standard form.', sections: [{ heading: 'Four temporal types', body: 'TOML supports offset date-time, local date-time, local date and local time. Use ISO 8601-like forms so tools can interpret them.' }], example: 'published = 1979-05-27T07:32:00Z\nbirthday = 1990-01-01', exercises: [basic('date-1', 'Add a date', 'Create birthday = 1990-01-01.', '', 'birthday = 1990-01-01', { birthday: '1990-01-01' }), ex('date-2', 'Repair the date', 'Remove the quotes around the date.', 'published = "1979-05-27T07:32:00Z"', 'published = 1979-05-27T07:32:00Z', { published: '1979-05-27T07:32:00Z' }, ['TOML has a native date/time value.', 'Remove the quotes.', 'Keep the ISO-style date and time intact.']), basic('date-3', 'Schedule a release', 'Create release = 2024-06-01.', '', 'release = 2024-06-01', { release: '2024-06-01' })] },
    { id: 'style', number: '13', title: 'Comments & Style', eyebrow: 'PRACTICE', summary: 'Make configuration easy for the next person to maintain.', sections: [{ heading: 'Comments explain why', body: 'Use comments for context, tradeoffs and safe defaults. Avoid comments that merely repeat the key.' }, { heading: 'Consistent formatting', body: 'Use spaces around equals, group related keys and keep tables in a logical order.' }], example: '# Public HTTP port\nport = 8080\n\n# Keep this false locally\ndebug = false', exercises: [basic('style-1', 'Add a comment', 'Create port = 8080. Comments are optional.', '# HTTP port\n', '# HTTP port\nport = 8080', { port: 8080 }), ex('style-2', 'Repair the style', 'Make this valid TOML.', 'port = 8080 # http port\ndebug = false', 'port = 8080 # http port\ndebug = false', { port: 8080, debug: false }, ['Inline comments are allowed.', 'Each setting stays on its own line.', 'The comment does not become data.']), basic('style-3', 'Write readable config', 'Create name = "demo" and version = "1.0".', '', 'name = "demo"\nversion = "1.0"', { name: 'demo', version: '1.0' })] },
    { id: 'errors', number: '14', title: 'Common Errors', eyebrow: 'DEBUG', summary: 'Learn to read parser feedback and fix the shape of your data.', sections: [{ heading: 'Typical traps', body: 'Watch for duplicate keys, repeated table definitions, unterminated strings, mixed array types and missing quotes.' }, { heading: 'Format confusion', body: 'TOML uses = and table headers. YAML often uses :, while JSON requires quoted keys and braces. Similar-looking syntax is not interchangeable.' }], example: '[server]\nhost = "localhost"\nport = 8080', exercises: [ex('err-1', 'Fix the quotes', 'Repair the missing quotes and brackets.', '[server]\nhost = localhost\nport = 8080', '[server]\nhost = "localhost"\nport = 8080', { server: { host: 'localhost', port: 8080 } }, ['String values need quotes.', 'The table header is already correct.', 'Keep port as a number.']), ex('err-2', 'Fix the duplicate', 'Keep only one name key with value demo.', 'name = "demo"\nname = "demo"', 'name = "demo"', { name: 'demo' }, ['A key may only be defined once.', 'Delete one duplicate line.', 'The final value should be demo.']), basic('err-3', 'Build valid config', 'Create [server] with host = "localhost".', '', '[server]\nhost = "localhost"', { server: { host: 'localhost' } })] },
    { id: 'pyproject', number: '15', title: 'pyproject.toml', eyebrow: 'REAL WORLD', summary: 'Use TOML to describe a small Python project.', sections: [{ heading: 'Project metadata', body: 'pyproject.toml gives tools a standard place to discover a project name, version, description and dependencies.' }, { heading: 'Tool configuration', body: 'The [tool] namespace gives individual tools room for their settings. Scripts map command names to Python callables.' }], example: '[project]\nname = "example"\nversion = "0.1.0"\ndescription = "Example project"\n\n[project.scripts]\nexample = "example:main"', note: 'This lesson focuses on the shape and ideas, not every packaging detail.', exercises: [ex('py-1', 'Add project metadata', 'Create [project] with name and version.', '', '[project]\nname = "example"\nversion = "0.1.0"', { project: { name: 'example', version: '0.1.0' } }, ['Open a project table.', 'Add two string values.', 'Use the example as your map.']), ex('py-2', 'Repair the script', 'Fix the project.scripts header.', '[project.scripts]\nexample = example:main', '[project.scripts]\nexample = "example:main"', { project: { scripts: { example: 'example:main' } } }, ['The callable path is text.', 'Use quotes around example:main.', 'The dotted header creates nested tables.']), basic('py-3', 'Describe your package', 'Create [project] with name = "demo" and description = "A demo".', '', '[project]\nname = "demo"\ndescription = "A demo"', { project: { name: 'demo', description: 'A demo' } })] },
  ],
}
